# Changelog

## 2.9.0 (2026-05-16)

- **Tips タブを追加**（拡張機能スクリプト同期）: 使い方やアップデート情報を日替わりランダム2件で表示。シャッフル・全件表示に対応
- **タブ順を変更**: 設定タブと Tips タブの順番を入れ替え（課題 → 教科書 → Tips → 設定）
- Tips に FIDO 認証（パスキー）の案内を追加

## 2.8.0 (2026-05-16)

- **TOTP コード確認機能**: ログイン画面で「現在のコードを表示」ボタンから 6 桁 TOTP コード（30 秒カウントダウン付き）と保存済みシークレットキーを表示
- **QR コードスキャナーを追加**: カメラで TOTP の QR コードを読み取り、シークレットキーを自動登録（ブラウザ拡張機能で生成した QR にも対応）
- **TOTP 自動入力失敗時のエラー表示を改善**: TOTP 入力失敗がパスワードエラーと誤表示される問題を修正（WebView に委ねるよう変更）
- **OTP ページ検出の精度向上**: username フィールドが非表示で password フィールドが存在するケースを OTP ページとして検出
- 拡張機能スクリプト同期（`src/auth-totp.js` の変更を反映）

## 2.7.0 (2026-05-14)

- **ログイン画面に TOTP（ワンタイムパスワード）設定を追加**
  - ログイン画面から直接 TOTP シークレットキーを登録可能に（設定画面を開く必要なし）
  - 保存済みなら「設定済み + 削除ボタン」、未保存なら「説明 + 入力フィールド + 保存ボタン」を表示
- **TOTP 自動入力の改善**
  - ログイン時に OTP フォームを検出し、保存済みシークレットからワンタイムパスワードを自動入力
  - WebView モードでも TOTP 自動入力に対応
  - OTP フォーム検出のリトライ機構を追加（最大3回、300ms間隔）
- **「ID/パスワード入力に戻る」後に自動ログインが再発するバグを修正**
  - `didAutoLogin` 状態を親ビューに引き上げ、画面再生成でリセットされないよう修正
- **セキュリティ説明を更新**
  - TOTP シークレットの保存方法（Keychain）と同一端末での 2FA に関する注意事項を追記

## 2.6.2 (2026-05-12)

- **拡張機能スクリプト同期**: TA 採点支援機能の CSS / i18n を同期（TA 採点支援自体はブラウザ拡張機能専用のためアプリ側では動作しません）

## 2.6.1 (2026-05-11)

- **Android 版リリースバナーを追加**（拡張機能スクリプト同期）

## 2.6.0 (2026-05-11)

- **ツリービュー拡張対応**（拡張機能スクリプト同期）
  - 授業資料をインデント・矢印・キーボード操作付きのツリー表示に変更
  - 設定でオン/オフ可能（デフォルトOFF）

## 2.5.0 (2026-05-11)

- **ファイル ドラッグ＆ドロップ対応**（拡張機能スクリプト同期）
  - 課題提出ページにドロップゾーンを追加し、D&Dでファイル添付が可能に
  - 設定でオン/オフ可能（デフォルトON）

## 2.4.0 (2026-05-11)

- **ホーム画面クイックアクション**: アプリアイコン長押しで締切が近い課題を最大4件表示。タップで課題ページに直接遷移
- 通知スケジュールの信頼性を改善: 通知の削除→再登録のレース回避、`UNTimeIntervalNotificationTrigger` に変更
- モーダル WebView のダウンロード処理を `WKDownload` ベースに改善

## 2.3.3 (2026-05-09)

- 通知スケジュールの信頼性を改善: 通知の削除→再登録の窓を最小化し、並行呼び出しの排他制御を追加

## 2.3.2 (2026-05-09)

- アプリ使用中（フォアグラウンド）でも通知がバナー表示されるよう修正
- 通知タップ時のリンク先をコーストップから課題ツールページに変更

## 2.3.1 (2026-05-01)

- ハンバーガーメニュー（☰）ボタンの重複表示を修正（拡張機能スクリプト同期）

## 2.3.0 (2026-05-01)

- **設定・通知画面を追加**
  - ツールバーにベルアイコンを追加し、タップで設定画面をシート表示
  - 通知タイミングのカスタマイズ: プリセット（10分前〜3日前）から追加・削除可能
  - 新着課題の通知: トグルで ON/OFF 切り替え、新しい課題が追加された時に即時通知
  - セキュリティ説明セクション: 認証情報の取り扱い（Keychain暗号化、開発者サーバーへの非送信）を明記
  - アプリ情報セクション: バージョン表示、GitHubリンク
- ログアウトボタンをツールバーから設定画面内に移動

## 2.2.0 (2026-05-01)

- **繰り返しメモ機能を追加**（拡張機能スクリプト同期）
  - メモに「毎週繰り返し」オプションを追加。完了チェック時に次週分のメモを自動生成
  - 繰り返しメモには「繰り返し」バッジを表示
  - 削除時の確認テキストを「繰り返しを停止しますか？」に変更
- プラットフォーム識別子 `window.__kulmsPlatform = 'ios'` を追加し、お問い合わせリンクをiOS専用フォームに変更

## 2.1.4 (2026-04-26)

- 上部ピン留めバー: システム警告バナー表示時にバーがヘッダーの裏に隠れるバグを修正（拡張機能スクリプト同期）

## 2.1.3 (2026-04-24)

- 課題リンクタップ時にパネルを自動で閉じるよう改善（拡張機能スクリプト同期）

## 2.1.2 (2026-04-24)

- 期限なしメモの表示位置を「その他」と「完了済み」の間に変更（拡張機能スクリプト同期）

## 2.1.1 (2026-04-24)

- 非JSONレスポンスで誤ってログアウト扱いされるバグを修正
- ページ読み込み時にキャッシュ期限切れでもタブ色分け・通知バッジを即座に適用するよう修正

## 2.1.0 (2026-04-24)

- モーダル WKWebView でファイル表示機能を実装
  - `decidePolicyForNavigationAction` で `target="_blank"` および `/access/` URL を検知
  - `presentModalWebView(url:)` で同一 WKWebsiteDataStore（Cookie共有）の新 WKWebView をモーダル表示
  - UINavigationController で閉じるボタン（×）とダウンロードボタン（arrow.down.circle）付き
  - WKWebView は PDF をネイティブにインライン表示可能
- `downloadAndShare(url:from:)` でファイルダウンロード → UIActivityViewController で外部アプリに共有
- ファイル名から UUID プレフィックスを除去（WKDownload パスと downloadAndShare の両方）
- `hasInitiallyLoaded` フラグ追加: モーダルを閉じた後に LMS トップページに戻るバグを修正（`LMSWebView.onAppear` の `loadPortal()` 二重呼び出し防止）

## 2.0.4 (2026-04-20)

- Android版テスター募集バナーを追加（拡張機能スクリプト同期）

## 2.0.3 (2026-04-20)

- App Store 審査用デモモードを追加
  - ログイン画面下部の「Demo Mode」ボタンからアクセス
  - 静的 HTML による LMS 風 UI + KULMS+ パネルのデモ表示
  - 課題・教科書・設定タブの操作、メモ機能、セクション折りたたみ等が体験可能
  - 本番環境への影響なし（独立した WKWebView で動作）

## 2.0.2 (2026-04-20)

- 外部リンク（target="_blank"）が開けない問題を修正
  - バージョンリンク、ホームページリンク等をタップすると Safari で開くよう修正

## 2.0.1 (2026-04-20)

- 拡張機能の画面幅制限を撤廃し、モバイルでも全機能を表示
- 教科書タブの科目順を曜日・時限順にソート
- NOW/NEXT バッジのテキストが科目名に混入するバグを修正

## 2.0.0 (2026-04-19)

- WebView版として再構築
- 拡張機能スクリプト全機能対応

## 1.0.0 (2026-04-19)

- 初回リリース
- WKWebView ベースの LMS 表示
- 拡張機能スクリプト注入 (kulms-shim)
- ECS-ID / SPS-ID ログイン（2FA 対応）
- パスワード暗号化保存
- 課題の締切通知
